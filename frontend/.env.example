# In development Vite proxies /api → backend (see vite.config.js)
# In production nginx proxies /api → backend
# Only needed if you deploy frontend separately without a proxy:
# VITE_API_BASE_URL=http://your-server-ip:3000
